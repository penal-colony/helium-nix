--- a/chrome/browser/helium_flag_choices.h
+++ b/chrome/browser/helium_flag_choices.h
@@ -38,6 +38,8 @@ namespace helium {
   constexpr const char kHeliumCompactLocationWidthCommandLine[] =
       "helium-compact-location-width";
   constexpr const char kHeliumZenModeCommandLine[] = "helium-zen-mode";
+  constexpr const char kMiddleClickPasteEnabledCommandLine[] =
+      "helium-middle-click-paste-enabled";
 }  // namespace helium
 
 #endif  /* CHROME_BROWSER_HELIUM_FLAG_CHOICES_H_ */
--- a/chrome/browser/helium_flag_entries.h
+++ b/chrome/browser/helium_flag_entries.h
@@ -46,4 +46,8 @@
      "Hides browser chrome (toolbar, tab strip, bookmarks bar) and reveals it "
      "on hover near the window edges. Helium flag.",
      kOsDesktop, FEATURE_VALUE_TYPE(features::kHeliumZenMode)},
+    {helium::kMiddleClickPasteEnabledCommandLine,
+     "Middle Click Paste",
+     "Controls middle click paste functionality. Helium flag.",
+     kOsLinux, FEATURE_VALUE_TYPE(features::kMiddleClickPasteEnabled)},
 #endif  /* CHROME_BROWSER_HELIUM_FLAG_ENTRIES_H_ */
--- a/chrome/browser/renderer_preferences_util.cc
+++ b/chrome/browser/renderer_preferences_util.cc
@@ -9,6 +9,7 @@
 #include <string>
 #include <vector>
 
+#include "base/feature_list.h"
 #include "base/logging.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/string_util.h"
@@ -143,6 +144,12 @@ void UpdateFromSystemSettings(blink::Ren
 #endif  // BUILDFLAG(USE_AURA)
 
 #if BUILDFLAG(IS_LINUX)
+  if (auto* feature_list = base::FeatureList::GetInstance();
+      feature_list && feature_list->IsFeatureOverridden(
+          features::kMiddleClickPasteEnabled.name)) {
+    prefs->middle_click_paste_allowed =
+        base::FeatureList::IsEnabled(features::kMiddleClickPasteEnabled);
+  } else
   if (auto* linux_ui = ui::LinuxUi::instance()) {
     prefs->middle_click_paste_allowed = linux_ui->PrimaryPasteEnabled();
   }
--- a/ui/base/clipboard/clipboard.cc
+++ b/ui/base/clipboard/clipboard.cc
@@ -29,6 +29,8 @@
 #include "url/gurl.h"
 
 #if BUILDFLAG(IS_LINUX)
+#include "base/feature_list.h"
+#include "ui/base/ui_base_features.h"
 #include "ui/linux/linux_ui.h"
 #endif
 
@@ -116,6 +118,13 @@ bool Clipboard::IsSupportedClipboardBuff
 // static
 bool Clipboard::IsMiddleClickPasteEnabled() {
 #if BUILDFLAG(IS_LINUX)
+  if (auto* feature_list = base::FeatureList::GetInstance();
+      feature_list && feature_list->IsFeatureOverridden(
+          features::kMiddleClickPasteEnabled.name)) {
+    return base::FeatureList::IsEnabled(
+        features::kMiddleClickPasteEnabled);
+  }
+
   if (auto* linux_ui = ui::LinuxUi::instance()) {
     return linux_ui->PrimaryPasteEnabled();
   }
--- a/ui/base/ui_base_features.cc
+++ b/ui/base/ui_base_features.cc
@@ -463,4 +463,6 @@ BASE_FEATURE_PARAM(int,
 
 BASE_FEATURE(kSplitViewLinkOpen, base::FEATURE_DISABLED_BY_DEFAULT);
 
+BASE_FEATURE(kMiddleClickPasteEnabled, base::FEATURE_ENABLED_BY_DEFAULT);
+
 }  // namespace features
--- a/ui/base/ui_base_features.h
+++ b/ui/base/ui_base_features.h
@@ -304,6 +304,9 @@ BASE_DECLARE_FEATURE_PARAM(int, kCompens
 COMPONENT_EXPORT(UI_BASE_FEATURES)
 BASE_DECLARE_FEATURE(kSplitViewLinkOpen);
 
+COMPONENT_EXPORT(UI_BASE_FEATURES)
+BASE_DECLARE_FEATURE(kMiddleClickPasteEnabled);
+
 }  // namespace features
 
 #endif  // UI_BASE_UI_BASE_FEATURES_H_
